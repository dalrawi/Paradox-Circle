<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <p>Hello, <%= @user.name %></p>
      </h1>
	  
	  <%= month_calendar do |date| %>
         <%= date.day %>
      <% end %>
    </section>
  </aside>
</div>
